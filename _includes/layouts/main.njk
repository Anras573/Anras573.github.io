<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ resume.basics.name }} - {{ resume.basics.label }}</title>

    <meta name="description" content="{{ resume.basics.summary }}">

    {% include 'components/bundle-css.njk' %}
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>